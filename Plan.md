当前已完成的工作

  ┌─────────────────────────────────┬────────────────────┬───────────────────────┐
  │              模块               │        状态        │         质量          │
  ├─────────────────────────────────┼────────────────────┼───────────────────────┤
  │ 三 Agent 双层循环架构           │ ✅ 代码完成        │ 已跑通但需要更多测试  │
  ├─────────────────────────────────┼────────────────────┼───────────────────────┤
  │ Stage-Aware Planning（5 阶段）  │ ✅ 刚完成          │ 未测试                │
  ├─────────────────────────────────┼────────────────────┼───────────────────────┤
  │ 段级编辑（FFmpeg enable）       │ ✅ 完成            │ 未测试                │
  ├─────────────────────────────────┼────────────────────┼───────────────────────┤
  │ CLIP+MLP 本地评估               │ ✅ 训练完成（81%） │ 用于 MCTS             │
  ├─────────────────────────────────┼────────────────────┼───────────────────────┤
  │ Storyboard 视觉压缩             │ ✅ 完成            │ 已验证可用            │
  ├─────────────────────────────────┼────────────────────┼───────────────────────┤
  │ 自部署 Qwen2.5-VL-72B           │ ✅ NPU 8 卡跑通    │ 推理较慢（~5s/token） │
  ├─────────────────────────────────┼────────────────────┼───────────────────────┤
  │ Critic 反馈传递 + seg_id 归一化 │ ✅ 修复            │ 未验证                │
  └─────────────────────────────────┴────────────────────┴───────────────────────┘

  缺什么（按优先级排序）

  P0：必须做（没有这些论文无法投稿）

  1. 跑通完整流程 + 修 bug

  当前代码做了很多改动但还没有完整跑一遍验证。需要先把整个 pipeline 稳定跑通，特别是：
  - Stage-Aware Planning 的 Director 是否能正确输出 stages
  - 段级编辑的 enable 是否真的只对目标段生效
  - Critic 反馈传递和 degraded 动作剔除是否正确工作
  - 最终输出视频的质量是否比之前改善

  2. 消融实验

  这是论文最核心的素材。PhotoAgent 和 PhotoArtAgent 都因为消融不充分被批评。需要：

  ┌───────────────────────┬─────────────────────────────────────────┬──────────────────────┐       
  │         消融          │                  对比                   │       证明什么       │       
  ├───────────────────────┼─────────────────────────────────────────┼──────────────────────┤       
  │ 三 Agent vs 单 Agent  │ 去掉 Director 和 Critic，只用 Editor    │ 多角色协作的价值     │       
  ├───────────────────────┼─────────────────────────────────────────┼──────────────────────┤       
  │ Stage-Aware vs 无阶段 │ Director 不输出 stages，Editor 自由生成 │ 专业阶段化规划的价值 │       
  ├───────────────────────┼─────────────────────────────────────────┼──────────────────────┤       
  │ 反思闭环 vs 纯分数    │ Critic 只给分数不给建议                 │ 结构化反馈的价值     │       
  ├───────────────────────┼─────────────────────────────────────────┼──────────────────────┤       
  │ MCTS vs 贪心          │ 不搜索，直接用 Editor 第一方案          │ 搜索的价值           │       
  ├───────────────────────┼─────────────────────────────────────────┼──────────────────────┤       
  │ 段级编辑 vs 全局编辑  │ 所有操作全视频应用                      │ 段级精细编辑的价值   │       
  ├───────────────────────┼─────────────────────────────────────────┼──────────────────────┤       
  │ CLIP+MLP vs VLM 评估  │ MCTS 用 VLM 评估替代本地模型            │ 本地评估的效率价值   │       
  └───────────────────────┴─────────────────────────────────────────┴──────────────────────┘       

  3. 简单基线对比

  PhotoArtAgent 因为"Lightroom Auto 效果差不多"被打 2 分。必须正面对比：

  ┌────────────────────────┬────────────────────────────────────────┬──────────────────┐
  │          基线          │                  描述                  │    为什么要比    │
  ├────────────────────────┼────────────────────────────────────────┼──────────────────┤
  │ 原始视频（不编辑）     │ 什么都不做                             │ 证明编辑有价值   │
  ├────────────────────────┼────────────────────────────────────────┼──────────────────┤
  │ FFmpeg auto-levels     │ 一行命令自动校正                       │ 零成本基线       │
  ├────────────────────────┼────────────────────────────────────────┼──────────────────┤
  │ 单轮 LLM 建议          │ LLM 看视频给一条建议，直接执行，不迭代 │ 证明闭环的价值   │
  ├────────────────────────┼────────────────────────────────────────┼──────────────────┤
  │ 纯 Perceiver（旧架构） │ 没有 Director/Critic，单层循环         │ 证明新架构的提升 │
  └────────────────────────┴────────────────────────────────────────┴──────────────────┘

  4. 人类评估

  从 PhotoAgent/PhotoArtAgent 的 review 来看，这是审稿人最关注的。不能等 rebuttal 才加。

  - 10-15 人 pairwise comparison
  - 200+ 票
  - VlogAgent vs 各基线
  - VLM Judge 与人类一致性验证

  P1：应该做（显著提升论文质量）

  5. Critic 训练（Phase 2b）

  这是区分 VlogAgent 和"纯 prompt engineering"的关键。PhotoArtAgent
  因为"没有训练=没有贡献"被全面拒稿。

  - 云 GPU 训练 Qwen-VL-7B + GRPO
  - 替代 Critic 的 VLM API 调用
  - 消融：训练 Critic vs 通用 VLM Critic

  6. 效率分析

  PhotoAgent 的 580 秒是被拒的重要原因。需要：
  - 各组件耗时 profiling（类似 PhotoAgent 做的表格）
  - 不同模拟次数的效率-质量曲线
  - 与其他 Agent 方法的运行时间对比

  7. 更多测试视频

  当前只用 1 段测试视频验证，完全不够。至少需要 25-40 段不同场景的视频：
  - Pexels 下载的（旅行/城市/自然/美食）
  - 不同画质（好的和差的）
  - 不同时长（10s / 30s / 1min / 2min）

  P2：可以做（锦上添花）

  8. 自监督演示学习（借鉴 EditDuet）
  - 自动探索生成 Director/Editor/Critic 的高质量 in-context 演示
  - 不微调也能提升决策质量

  9. 更丰富的编辑工具
  - 转场效果（crossfade）
  - AI 工具（超分辨率、物体移除）

  10. VlogEdit 评估基准
  - 贡献一个视频编辑评估数据集（类似 PhotoAgent 的 UGC-Edit）

  建议的执行顺序

  第 1 周：稳定跑通 + 批量测试
    ├── 修完剩余 bug
    ├── 在 5-10 段不同视频上跑完整流程
    ├── 收集运行日志，确认各模块正常工作
    └── 效率 profiling

  第 2 周：消融实验 + 基线对比
    ├── 跑 6 组消融实验（每组 10+ 段视频）
    ├── 跑 4 个简单基线
    └── 整理数据

  第 3 周：Critic 训练 + 评估
    ├── Phase 2b（云 GPU 训练）
    ├── 集成到系统
    └── 训练 vs 未训练的消融对比

  第 4 周：人类评估 + 论文撰写
    ├── 设计用户研究
    ├── 收集 200+ 票
    ├── 开始写论文
    └── 准备可视化 case study